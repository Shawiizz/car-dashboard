import { GearRatioPipe } from './gear-ratio.pipe';

describe('GearRatioPipe', () => {
  it('create an instance', () => {
    const pipe = new GearRatioPipe();
    expect(pipe).toBeTruthy();
  });
});
